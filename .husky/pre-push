if command -v pnpm >/dev/null 2>&1; then
  pnpm check:pre-push
elif command -v corepack >/dev/null 2>&1; then
  corepack pnpm check:pre-push
else
  echo "pnpm is not available. Install pnpm or enable Corepack." >&2
  exit 127
fi
